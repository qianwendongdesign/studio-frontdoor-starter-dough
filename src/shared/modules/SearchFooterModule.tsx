/**
 * SearchFooterModule — Search pill + cart pill floating footer.
 *
 * Usage:
 *   import { SearchFooterModule } from '@shared/modules/SearchFooterModule'
 *   <SearchFooterModule />
 *
 * To customize beyond props, eject: copy Footer.tsx into your page folder
 * and modify the search and cart layout directly.
 */

import { Footer } from '@shared/components/Footer'

export function SearchFooterModule() {
  return <Footer />
}
