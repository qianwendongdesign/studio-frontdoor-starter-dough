/**
 * SpotlightModule — Hero video/image with promotional copy + CTA.
 *
 * Usage:
 *   import { SpotlightModule } from '@shared/modules/SpotlightModule'
 *   <SpotlightModule />
 *
 * To customize beyond props, eject: copy Spotlight.tsx into your page folder
 * and modify the media, copy, and CTA directly.
 */

import { Spotlight } from '@shared/components/Spotlight'

export function SpotlightModule() {
  return <Spotlight />
}
