/**
 * FourPackModule — Tab-filtered store grid (2x2) with crossfade images.
 *
 * Usage:
 *   import { FourPackModule } from '@shared/modules/FourPackModule'
 *   <FourPackModule />
 *
 * To customize beyond props, eject: copy SimilarSection.tsx into your page folder
 * and modify the tab data, store data, and layout directly.
 */

import { SimilarSection } from '@shared/components/SimilarSection'

export function FourPackModule() {
  return <SimilarSection />
}
